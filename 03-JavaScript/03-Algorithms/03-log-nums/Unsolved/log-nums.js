// Write code to print all numbers from 1 to `num`
// Assume `num` will be a positive number

var logNums = function(num) {
    arr = [];
    while(num >= 1) {
       arr.unshift(num);
       num = num -1;

//unneccessary code
       

    

        

    }
    for(i=1; i<= arr.length; i++){
        console.log(i);
    }
    
};


// first when number comes in push that to Array, then decrease by 1 name that a method and 