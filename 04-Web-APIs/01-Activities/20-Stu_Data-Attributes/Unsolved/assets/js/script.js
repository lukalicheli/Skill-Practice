var container = document.querySelector(".container");

container.addEventListener("click", function(event) {
  var element = event.target;

  // TODO: Complete function. start out by writing out some pseudo-code and
  // discuss it with your partner. Coming up with steps that will work just as
  // important as writing the code. If you only have enough time to get your
  // pseudo-code figured out, then that is the best use of the time.
});
