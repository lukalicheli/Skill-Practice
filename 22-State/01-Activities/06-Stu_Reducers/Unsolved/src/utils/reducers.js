// TODO: add actions which handle adding a student and removing a student. Should pass all tests when solved correctly.
export default function reducer(state, action) {
  switch (action.type) {
    default:
      return state;
  }
}
