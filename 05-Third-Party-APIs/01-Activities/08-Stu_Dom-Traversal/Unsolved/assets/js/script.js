// Ensure the document is rendered when manipulating DOM elements
// Assemble: Create/select DOM elements
var rootEl = $('#root');

// TODO: Starting from the root element, `<main id="root">`, select all the
// boxes and change the background color to white.

// TODO: Select the last `<ul>` or the third row and assign it to a variable
// named rowThree.

// TODO:Select the children of rowThree, then select the first box and set its
// text to "O".
