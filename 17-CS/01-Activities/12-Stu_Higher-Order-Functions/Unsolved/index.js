const grades = [56, 78, 99, 85];

// TODO: Complete the 'count' function below.
function count(accumulator, currentValue, index, array) {
    
}

// TODO: Pass the 'count' into the reduce method along with an initial value for accumulator.
const totalPassing = grades.reduce();

// expected result is 3
console.log(gradeAverage);
